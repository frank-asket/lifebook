{
  "name": "lifebook",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "node scripts/start-all.js dev",
    "build": "npm run build --workspace=web",
    "start": "node scripts/start-all.js start",
    "lint": "npm run lint --workspace=web",
    "test:backend": "python3 -m unittest backend/tests/test_api.py"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  }
}
