{
  "name": "lifebook-backend",
  "version": "0.1.0",
  "private": true,
  "description": "LifeBook agent pipeline + API — powered by Python (FastAPI).",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8787 --reload",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8787",
    "test": "PYTHONPATH=. python3 tests/test_api.py",
    "build": "echo 'Backend ready'",
    "typecheck": "PYTHONPATH=. python3 tests/test_api.py"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@clerk/backend": "^3.17.1",
    "@supabase/supabase-js": "^2.116.0"
  }
}
